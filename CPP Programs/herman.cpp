#include<iostream>
#include<iomanip>
#include<cmath>
using namespace std;
int main(){
    int r;
    cin >> r;
    cout << fixed << setprecision(6) << M_PI*pow(r,2) << endl;
    cout << fixed << setprecision(6) << 2*pow(r,2);
}
